<UserControl x:Class="Gallium.UserControls.AutoCompleteTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Gallium.UserControls"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <TextBox x:Name="SearchBox" HorizontalAlignment="Stretch" VerticalAlignment="Top" ToolTip="Szukaj" Panel.ZIndex="1"/>
        <!--<Rectangle Name="Suggestions" HorizontalAlignment="Stretch" VerticalAlignment="Top" Margin="0,18,0,0">-->
            
        <!--</Rectangle>-->
        <ListBox  Name="Suggestions" HorizontalAlignment="Stretch" VerticalAlignment="Top" Margin="0,18,0,0"/>
        <!--<ComboBox x:Name="ComboBox" HorizontalAlignment="Stretch" VerticalAlignment="Top" Visibility="Visible" Margin="0,0,0,0"/>-->
    </Grid>
</UserControl>
